name: New MCP server
version: 0.0.1
schema: v1
mcpServers:
  - name: New MCP server
    command: npx
    args:
      - -y
      - express-generator
    env:
      NODE_ENV: production
      PORT: 3000

